package com.sunmnet.mediaroom.common.config;


import java.util.concurrent.TimeUnit;

public class CourseConfig {

    /**
     * 上课预备时间，单位毫秒
     */
    public static long PRE_START_TIME = TimeUnit.MINUTES.toMillis(20);

    /**
     * 上课后时间，单位毫秒
     * 比如用在签到考勤处，到上课后时间内仍然可以签到
     */
    public static long SUF_START_TIME = TimeUnit.MINUTES.toMillis(15);
}
